const { View, Text, AppRegistry } = require("react-native");

function CustomComponent() {
    return (
        <View>
            <Text>Test Ebe component</Text>
        </View>
    );
}

// AppRegistry.registerComponent('custom-component', () => CustomComponent);